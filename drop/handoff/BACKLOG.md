# Related backlog — not part of the corrected core spec unless re-confirmed for this implementation

## Existing authenticated browser / Panerelay

Prior work explored putting Panerelay under Nano so existing logged-in Chrome tabs can be used while retaining Nano's Leader/Follower UX.

Keep as a candidate follow-on integration.

## ChatGPT site augmentation

Separate `chatgpt-exporter-test.zip` exists.

Related work includes ChatGPT width/composer augmentation, persistent page-side scripts, deterministic exporter invocation, and bulk export.

Treat this as its own vertical slice after core config/runtime work unless explicitly pulled into the current implementation.

## Personal extension / multiple model sites

Earlier related discussion explored one extension/runtime for persistent augmentation across model-provider sites and broader conversation utilities.

Do not fold that entire product into the Nano core modernization by accident.
