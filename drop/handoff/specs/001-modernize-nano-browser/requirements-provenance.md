# Requirements provenance

| Requirement | Source |
|---|---|
| FR-001 | User repeatedly described the old settings/configuration flow as a nightmare/pain to configure. |
| FR-002 | User specifically complained that configuration was painful to save and reuse. |
| FR-003 | User wants Nano's existing Leader/Follower / Planner/Navigator flow maintained while internals are modernized. |
| FR-004 | User explicitly suggested the inner model can emit/decide when passes go back to the outer model instead of relying only on explicit turn count. |
| FR-005 | Current user statement: “must retain the ability for deterministic scaling.” Exact semantics intentionally unresolved. |
| FR-006 | User asked: “Is there any way we can have a log of it as it moves along?” |
| FR-007 | User wants the existing set-of-marks interaction retained. |
| FR-008 | User said the set-of-marks can “get a little out of hand” and asked how it will be generated. |
| FR-009 | User proposed optional SAM 3 as a possible second mark generator. |
| FR-010 | User explicitly asked for the rewrite with LangGraph and for the other obsolete internals/providers to be upgraded/current. |

No other Functional Requirements should be added without explicit user approval.
